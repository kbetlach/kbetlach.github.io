<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Devour!{{else}}Order Up!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Trash It!</button>
</li>